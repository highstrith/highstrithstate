# 项目协作规则

本文件适用于 `/Volumes/E61--/网站/site` 及其子目录。

## 固定文档

每次任务开始前，依次阅读：

1. `AGENTS.md`
2. `产品需求.md`
3. `软件架构.md`
4. `当前进度.md`

这三份项目文档是需求、架构和交付状态的唯一固定信息源；重要决定不得只保留在聊天记录中。

## 执行流程

1. 根据上述文档写出简短执行计划，至少覆盖目标、涉及文件或模块、步骤、预期结果与风险。
2. 明确的功能、修改或修复指令视为授权执行；其余会影响方向的方案先征求确认。
3. 代码或行为变化先更新或新增真实行为测试，再实现最小改动。
4. 完成后运行相关测试、语法检查和必要的界面验证；未解决事项必须记录。
5. 同步更新 `当前进度.md`，并在需求或架构变化时更新相应文档。
6. 每个完成的改动集都创建清晰的 Git commit。项目已经存在 Git 仓库，不要重新初始化或重写历史。

## 工程纪律

- 文本文件使用 `apply_patch` 修改；不得覆盖用户已有的无关变更。
- 媒体仅在用户提供或明确授权的范围内复制到 `assets/`。
- GitHub Pages 是公开静态站；`serve.js` 仅用于本机编辑与上传服务，不能把本地管理能力误认为线上能力。
- 如有必要创建项目专属 Skill，放在 `skill/` 目录，并在本文档登记用途与维护方式。
- 遇到测试失败或异常行为，先定位根因，再修改实现。
