# 软件架构

## 技术栈与部署

- 静态前端：单文件 `index.html`（HTML、CSS、原生 JavaScript）。
- 本地服务：Node.js `serve.js`，提供作品资料和本地上传管理 API。
- 数据：`data/works.json` 保存全部作品目录元数据。
- 部署：GitHub Pages 发布静态文件；线上不运行 `serve.js`。

## 目录职责

| 路径 | 职责 |
| --- | --- |
| `index.html` | 公共作品集页面、样式、交互、作品数据渲染与语言切换。 |
| `admin.html` | 本地作品管理界面。 |
| `serve.js` | 本地静态服务及上传、编辑、删除 API。 |
| `data/works.json` | 全部作品的标题、分类、描述、视频、移动端视频与封面路径；上传作品和内置精选作品统一维护。 |
| `assets/hero-digital-human-full.jpg` | 从用户提供的单张数字人原图转换的网页用照片；通过 CSS 与背景融合。 |
| `assets/hero-studio-red.jpg` | 生成并压缩的暗红影棚氛围底图，不包含人物；作为 Hero 背景。 |
| `assets/works/` | 当前精选作品视频。 |
| `assets/posters/` | Hero 与精选作品封面。 |
| `assets/uploads/` | 本地上传作品资源及封面。 |
| `assets/social/` | 用户提供的微信、抖音、小红书二维码。 |
| `tests/media-integration.mjs` | 媒体文件存在性与页面引用检查。 |
| `tests/media-smoke.py` | Playwright 本地页面交互与加载冒烟测试。 |
| `tests/site-redesign-smoke.py` | 桌面、平板、手机与本地文件模式下的布局和交互冒烟测试。 |

## 页面与数据流

1. `index.html` 在本地服务环境优先读取 `/api/works`，否则读取 `data/works.json`；直接以 `file://` 打开导致读取受限时回退到内置 `baseWorkData` 的 13 个作品快照。首页客户端分页，每页渲染 8 个条目；维护作品目录时要同步快照，测试会比对两者标题。
2. 首页 Hero 使用用户提供的单张数字人照片和生成的影棚背景，不加载背景视频；卡片视频使用 `data-src` 延迟提供源路径，`syncWorkVideos()` 只为接近视窗的卡片加载并静音播放，超出缓冲区后卸载。完整播放器只由点击、触摸或键盘触发。
3. 作品封面优先读取作品条目 `poster` 字段；视频未加载时由封面保持稳定画面。
4. 社交二维码按钮通过 `data-qr-image`、`data-qr-title` 与 `data-qr-account` 填充页面级悬浮预览与原生 `<dialog>`；预览使用固定定位并限制在视窗内，哔哩哔哩为外部安全链接。
5. `serve.js` 只在本机环境服务 `/api/works`；线上静态站使用随仓库发布的 `data/works.json`。
6. 本地管理页读取完整作品目录；作品卡片提供海报、桌面悬浮预览和视频弹窗播放；`PUT /api/works/:id` 允许编辑所有作品的文字元数据，仅允许上传作品替换视频或封面。

## 架构约束

- 不为社交账号臆造链接：二维码或链接必须由用户提供或确认。
- 所有面向公开站的媒体必须位于仓库的 `assets/` 内，不能引用用户电脑上的绝对路径。
- 作品自动播放必须保持 `muted`、`playsinline`，并尊重 `prefers-reduced-motion`；完整播放器的用户手动播放不受减少动态效果限制。
- 背景音乐禁止随页面交互自动启动，只由音乐开关明确控制，初始状态保持暂停。
- 交互式弹窗使用可键盘关闭的原生 `<dialog>`，图标按钮必须有可访问名称。
